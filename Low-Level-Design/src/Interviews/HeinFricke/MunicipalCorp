class MunicipalCorp{

    ArrayList<String> hospitalsWeHave=new ArrayList<>();
//This Hashmap will store the pincode and Hospital's name

    Hashmap<String,int> PincodeHospitalList;
    addHospital(){
        hospitalsWeHave.add("Hospital Name");
     }
     editHospital{

     }
     removeHospital(){
         hospitalsWeHave.removeHospital("Hospital Name");
    }
    searchHospital(int pincode){
        if(PincodeHospitalList.contains(pincode)){
            return  Hospital's Name;
        }
        else{
            //Ask to put another nearby Pincode
        }
    }
    //For this we will also keep a Hashmap<String, int> paitentAdmitted <- String will be Hospital and int will be Paitent Count of that Hospital

    public int totalNumberOfPaitentAdded(){
        //Run a loop over each hospital and check
        and can fetch and add
        return TotalPaitent;
    }

        //For this we will also keep a Hashmap<String, int> paitentDischarged <- String will be Hospital and int will be Paitent Count of that Hospital

    public int totalNumberOfPaitentDischarged(){
        //Run a loop over each hospital and check
        and can fetch and add
        return TotalPaitent;
    }
}
