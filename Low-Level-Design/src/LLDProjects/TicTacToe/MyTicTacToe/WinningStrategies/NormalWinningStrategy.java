package LLDProjects.TicTacToe.MyTicTacToe.WinningStrategies;

import LLDProjects.TicTacToe.MyTicTacToe.Board;
import LLDProjects.TicTacToe.MyTicTacToe.Move;

public class NormalWinningStrategy implements WinningStrategies{

    @Override
    public boolean checkWinner(Board board, Move lastMove) {
        //if(cell == either cover all columns,rows,diagonal in straight line, return true, else false
        return false;
    }
}
